#include <iostream>

struct Point { 
    int x;
    int y; 
    bool blocked; 
};
// create point and detirmine if blocked or not using depth, remeber
// to account for object distance in creation
// use speed to detirmine time in between poin creation 


int main() { 
int distance;
int speed;
int xAxis [20];
int yAxis [20]; 

do {
    //create points 
} while(distance > 10);
if (distance < 10) {
    //mark blocked points 
};

    return 0;
}